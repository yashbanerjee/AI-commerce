services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aiebot
    ports:
      - "5433:5432"
    volumes:
      - aiebot_pg:/var/lib/postgresql/data

volumes:
  aiebot_pg:
